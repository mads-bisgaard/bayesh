[project]
name = "bayesh"
version = "0.0.0"
dependencies = [
    "bashlex",
    "pydantic",
    "pydantic-settings",
    "typer"
]

[tool.setuptools.packages.find]
include = ["bayesh"]

[project.scripts]
bayesh = "bayesh.cli:cli"
